apiVersion: v1
kind: ConfigMap
metadata:
  name: report-config
  namespace: app
data:
  RESULT_SERVICE_URL: "http://result-service.app.svc.cluster.local:8001"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-service
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: report-service
  template:
    metadata:
      labels:
        app: report-service
    spec:
      containers:
        - name: report-service
          image: minyong1234/report-service:v1   # ← 수정
          ports:
            - containerPort: 8000
          env:
            - name: RESULT_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: report-config
                  key: RESULT_SERVICE_URL
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: app
spec:
  selector:
    app: report-service
  ports:
    - port: 8003
      targetPort: 8000
